[pytest]
# Collect only the real suite. Without this, a local PyInstaller build under
# dist/ (which copies app/*.py into _internal/) gets collected too and pytest
# aborts on duplicate module basenames — i.e. building the app breaks the tests.
testpaths = tests
norecursedirs = build dist* .venv .git scratch
